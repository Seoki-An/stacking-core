[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "stacking-core"
version = "0.1.0"
description = "Python bindings for stacking-core"
requires-python = ">=3.12"
dependencies = ["numpy>=1.26"]
